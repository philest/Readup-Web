<!DOCTYPE html>
<html>
<head>
  <title> Assessments Portal </title>
</head>
<body>
  <table>
    <tbody>
      <% @teachers.each do |t| %>
        <tr>
          <td><%= t.user.name %></td>
          <td><%= t.user.email %></td>
          <td><%= t.signature %></td>
          <td><%= link_to 'classrooms', classrooms_by_teacher_path(t) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>