{
  "scripts": {
    "postinstall": "cd client && yarn install",
    "build:test": "cd client && yarn run build:test"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "name": "birdup",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "webpack": "^3.0.0",
    "webpack-dev-server": "^2.5.0"
  }
}
